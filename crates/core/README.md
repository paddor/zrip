# zrip-core

Shared types, entropy coders, and SIMD primitives for the [zrip](https://crates.io/crates/zrip) encode/decode crates.

This crate is an internal building block. Use `zrip` directly unless you need only the shared definitions.
